dbt_f1_transformations:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Use a local DuckDB file; relative to the repo root.
      # You can override via the DBT_DUCKDB_PATH env var.
      path: "${DBT_DUCKDB_PATH:-f1_local_warehouse.duckdb}"
      threads: 1
